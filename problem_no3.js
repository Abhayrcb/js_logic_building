

function calculateCircleArea(radius) {
    const pi = Math.PI;
    let area = pi*radius*radius;

    return radius,area;
}

let radius,area = calculateCircleArea(5)

console.log(`radius=${radius},area=${area}`); // Expected output: 78.53981633974483